# Post
<%= @index %>
# User
<%= link_to @user.name, "/users" %>

# CommentCounnt
<%= @post.comments.count %>

# LikesCount
<%= @post.likes.count %>

# Post Text
<%= @post.text %>

<% @post.comments.each do |comment| %>
  # User
  <%= comment.user.name %>
  # Comment
  <%= comment.text %>
<% end %>